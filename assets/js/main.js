const toggle=document.querySelector('.nav-toggle');const links=document.querySelector('.nav-links');if(toggle){toggle.addEventListener('click',()=>links.classList.toggle('open'))}
document.querySelectorAll('[data-investor-open]').forEach(b=>b.addEventListener('click',()=>document.querySelector('.investor-gate')?.classList.add('open')));document.querySelectorAll('[data-investor-close]').forEach(b=>b.addEventListener('click',()=>document.querySelector('.investor-gate')?.classList.remove('open')));
document.querySelectorAll('form[data-demo-form]').forEach(form=>form.addEventListener('submit',e=>{const isLocal=location.protocol==='file:';if(isLocal){e.preventDefault();const notice=form.querySelector('.notice');if(notice){notice.textContent='Form validated successfully. Deploy on Netlify to activate secure submissions, or use the email/WhatsApp links provided.';notice.classList.add('show')}}}));
const y=document.querySelector('[data-year]');if(y)y.textContent=new Date().getFullYear();
